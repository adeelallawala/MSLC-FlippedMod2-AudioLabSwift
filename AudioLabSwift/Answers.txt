1. The program will not work properly if you make the FFT Magnitude buffer a larger array unless you change the buffer size to be higher because the FFT Magnitude buffer size has to be half of the time data buffer size. It will work if you just increase the overall buffer size.

2. It is better to pause the audioManager object instead of deallocating it if you are going to be navigating to that page often. And since our app only has one function, we can just pause our audioManager object for when the user navigates to that page again.
